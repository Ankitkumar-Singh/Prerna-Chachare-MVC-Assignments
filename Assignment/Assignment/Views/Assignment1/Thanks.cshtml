@model Assignment.Models.GuestResponse

@{
    ViewBag.Title = "Assignment1-Thanks";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="thanks container p-5">
    <div class="card p-4">
        <div class="text-center">
            @*@{
                    try
                    {
                        WebMail.SmtpServer = "smtp.gmail.net";
                        WebMail.SmtpPort = 587;
                        WebMail.EnableSsl = true;
                        WebMail.UserName = "prerna.chachare98@gmail.com";
                        WebMail.Password = "";
                        WebMail.From = "prerna.chachare98@gmail.com";
                        WebMail.Send("prerna.chachare98@gmail.com", "RSVP Notification", Model.Name + " is " + ((Model.WillAttend ?? false) ? "" : "not") + "attending");
                        WebMail.Send(Model.Email, "RSVP Notification", "Hello " + Model.Name + ". It's great that you're coming.");
                    }
                    catch (Exception)
                    {
                        @:<b>Sorry - we couldn't send the email to confirm your RSVP.</b>
                    }
                }*@
            <h1>Thank you, @Model.Name!</h1>
            <div class="lead">
                @if (Model.WillAttend == true)
                {
                    @:It's great that you're coming. The drinks are already in the fridge!
                }
                else
                {
                    @:Sorry to hear that you can't make it, but thanks for letting us know.
                }
            </div>
            <div class="btn btn-success backToRSVP">
                @Html.ActionLink("Back to RSVP", "RSVPForm")
            </div>
        </div>
    </div>
</div>